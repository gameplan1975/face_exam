<h1>つぶやき一覧</h1>
    <% @blogs.each do |blog| %>
        <tr>
        <td><%= blog.content %></td>
        <td><%= link_to '編集', edit_blog_path(blog) %></td>
        <td><%= link_to '削除', blog, method: :delete %></td>
        </tr>
    <% end %>
